{
  "name": "huobao-drama-frontend",
  "version": "2.0.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --host 0.0.0.0 --port 3013",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "generate:dist": "node scripts/write-build-meta.mjs && nuxt generate && node scripts/sync-dist.mjs",
    "backup:dist": "node scripts/backup-dist.mjs",
    "restore:dist": "node scripts/restore-dist.mjs",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.17.5",
    "vue": "^3.5.13",
    "vue-router": "^4.5.1",
    "vue-sonner": "^1.2.4",
    "lucide-vue-next": "^0.511.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
